import {StyleSheet} from 'react-native';
import myColors from '../../styles/myColors';

const cuttingStyles = StyleSheet.create({
  ytimg: {
    width: 239,
    height: 152,
    borderRadius: 12,
    marginBottom: 16,
  },
});
export default cuttingStyles;
